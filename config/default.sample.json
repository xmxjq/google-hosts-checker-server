{
    "serverConfig": {
        "checkCron": "every 1 minutes",
        "scanConcurrency": 50,
        "serverBaseUrl": "http://xxxx:8160"
    },
    "clientConfig": {
        "clientId": "default-test-id-01",
        "outputCron": "every 1 minutes",
        "submitCron": "every 30 seconds",
        "scanConcurrency": 50
    }
}